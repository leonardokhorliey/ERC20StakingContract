

const Landing = ({launchApp}) => {

    return (
        <>
        <h2>
            Welcome to Ebube's BubToken (BBT) Stake
        </h2>

        <button className= "landing-button" onClick= {launchApp}>
            START
        </button>
        </>
    )
}

export default Landing;